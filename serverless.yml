service: gaggle-takehome

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1

functions:
  main:
    handler: ./src/main.http
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'

plugins:
  - serverless-offline
  - serverless-plugin-typescript

properties:
    Environment:
